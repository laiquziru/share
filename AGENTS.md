# AGENTS.md

用于减少大模型常见编码错误。可与项目特定指令合并使用。

## 规则优先级

按以下优先级执行：

1. **硬约束**：安全、权限、数据完整性、用户明确要求，以及不得覆盖用户已有修改。
2. **默认策略**：先理解代码、最小修改、复用现有模式、验证结果。
3. **工作习惯**：计划、子代理、经验沉淀等。运行环境不支持时，用等价的简短文本记录替代。

当规则冲突时，优先遵守更高优先级的规则，并在最终说明中指出重要取舍。

## 1. 开始前理解问题

- 先阅读会被修改的代码、相关测试、调用链和项目级指令，再选择实现方式。
- 明确关键假设。低风险、不会改变接口或数据行为的歧义，可以采用合理假设并记录。
- 涉及公共 API、数据库结构、数据迁移、权限、安全、破坏性操作或不可逆行为时，必须先询问。
- Bug 修复和有明确正确答案的任务应自主推进，不要因为普通实现细节反复询问。
- 如果存在多种可行方案，选择与现有代码最一致且影响最小的方案；只有取舍会影响接口、数据或架构时才要求用户决策。

## 2. 简洁优先

使用以下顺序选择实现方式：

1. 确认需求确实需要该代码；不需要就不加。
2. 复用代码库已有的 helper、util、组件和模式。
3. 优先使用标准库。
4. 优先使用平台原生能力。
5. 使用已有依赖；不要为几行代码引入新依赖。
6. 选择足够解决当前问题的最小实现。

不要添加未要求的功能、一次性抽象或没有证据支持的灵活性。

不要为没有证据支持的极端场景增加复杂处理。但以下内容不可省略：

- 信任边界的输入校验；
- 网络、文件、数据库和其他外部依赖的错误处理；
- 防止数据丢失的保护；
- 安全措施；
- 基础无障碍支持；
- 用户明确要求的行为。

如果采用了存在已知天花板的简单方案，使用 `debt:` 注释说明限制和升级路径。例如：

```text
debt: 当前使用全局锁；吞吐量成为瓶颈时改为每账户锁。
```

自问：资深工程师会认为这段代码明显过度复杂吗？如果会，简化本次新增代码。

## 3. 精准修改

- 只修改完成请求、保持一致性或修复本次改动所必需的内容。
- 不要顺手重构无关代码、修改相邻注释或统一无关格式。
- 匹配项目现有风格，即使个人偏好不同。
- 不要删除预先存在的死代码，除非用户明确要求。
- 删除因本次改动产生的无用导入、变量、函数和配置。
- 不要覆盖或回退用户已有的未提交修改。

每一处行为或结构修改都应能说明它与用户请求、必要的兼容性处理或本次改动引发的问题之间的关系。

## 4. 目标驱动和验证

把任务转化为可验证的成功标准：

- 添加验证：为无效输入补充测试，并确认测试通过。
- 修复 Bug：先建立能够复现问题的测试，再修改实现并确认测试通过。
- 重构：确认重构前后的行为测试都通过。

完成前：

1. 运行与改动直接相关的测试。
2. 根据影响范围运行类型检查、静态检查或完整测试。
3. 查看失败日志和关键运行结果，必要时继续修复。
4. 检查最终 diff，确认没有无关改动或遗留代码。

没有测试或其他事实证据时，不要声称任务已经验证完成；应明确说明未能验证的部分及剩余风险。

## 5. 工作流

对于满足以下任一条件的任务，先写简短计划并逐项更新状态：

- 预计超过三个步骤；
- 涉及三个以上文件；
- 涉及接口、数据、架构或跨模块行为；
- 验证本身需要多个阶段。

计划格式：

```text
1. [步骤] → 验证：[检查]
2. [步骤] → 验证：[检查]
3. [步骤] → 验证：[检查]
```

如果运行环境支持计划、todo 或子代理工具，可以使用它们；不支持时直接在回复中维护同等简短的状态记录。

一个子代理只负责一个明确任务，例如代码探索、测试分析或独立实现。只有在能够减少主上下文负担或真正并行时才使用子代理。

如果实现过程中发现原计划不再适用，先重新评估范围和成功标准，再继续修改。

## 6. 追求优雅，但限制范围

- 简单、显而易见的修复不需要额外重构。
- 如果修复方案明显重复、脆弱或违反现有架构，只重写本次新增代码，使其保持清晰和可维护。
- 不要因为“更优雅”而扩大到无关模块。
- 交付前检查：是否存在更小、同样可靠且更符合现有代码风格的方案。

## 7. 经验沉淀

只有在项目允许且确实形成可复用规则时，才更新 `tasks/lessons.md`。

记录范围：

- 用户明确纠正、且未来可能再次发生的工程规则；
- 项目特有、可复用的约束；
- 能减少同类错误的验证方式。

不要记录一次性偏好、隐私、业务数据或没有复用价值的上下文。若目录或文件不存在，不要为了单次任务强行创建它。

## 8. 交付说明

最终回复应简要说明：

- 修改了什么；
- 修改原因或根因；
- 运行了哪些验证及其结果；
- 尚未验证的部分、已知限制或剩余风险。

如果无法完成某项工作，应直接说明阻塞原因和需要的外部输入，不要把未完成的工作表述为已完成。
